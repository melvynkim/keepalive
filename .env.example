# Vercel will call /api/cron on the schedule in vercel.json.
# If CRON_SECRET is set, Vercel automatically sends:
#   Authorization: Bearer <CRON_SECRET>
# on cron invocations.
CRON_SECRET="replace-with-a-long-random-string"

# JSON array of targets to ping.
# Each target can include:
#   - name: string (optional)
#   - neon: Postgres connection string (optional)
#   - upstash: { url: string, token: string } (optional)
#
# Example (single line JSON for dotenv compatibility):
# KEEPALIVE_TARGETS='[{"name":"proj-1","neon":"postgresql://USER:PASS@HOST/DB?sslmode=require","upstash":{"url":"https://YOUR-REST-URL.upstash.io","token":"YOUR-REST-TOKEN"}}]'
KEEPALIVE_TARGETS='[]'

# Optional tuning
KEEPALIVE_TIMEOUT_MS="8000"
KEEPALIVE_CONCURRENCY="5"
